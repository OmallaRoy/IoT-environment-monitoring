{
  "build": {
    "builder": "python",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn iot_dashboard.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "onFailure"
  }
}
